import 'package:flutter/material.dart';

class UiPalette {
  const UiPalette._();

  // Brand-aligned palette to match the Masarat Wasel identity.
  static const Color primary = Color(0xFF1E508E); // deep blue
  static const Color secondary = Color(0xFF0F172A); // dark text
  static const Color gray = Color(0xFF6B7280); // neutral gray
  static const Color background = Color(0xFFF5F8FC);
  static const Color surface = Colors.white;
}
