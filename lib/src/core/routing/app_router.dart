import 'package:msaratwasel_user/src/app/state/app_controller.dart';

/// Placeholder for a future GoRouter configuration.
///
/// Navigation is currently handled by AppController with the RootShell/Indexed
/// stack approach, without a declarative router. Keep this file to wire up
/// GoRouter later if deep links or web routing are required.
class AppRouter {
  AppRouter(this.controller);

  final AppController controller;
}
